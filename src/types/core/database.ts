// Placeholder until generated for real against your live Supabase project:
//   supabase gen types typescript --project-id your-project-ref > src/types/core/database.ts
//
// The hand-authored types in entities.ts (Profile, Project, Document,
// etc.) are the source of truth for what the schema SHOULD look like —
// this file just needs to structurally match after generation. Using
// `any` here rather than a hand-rolled Database type avoids the two
// definitions silently drifting apart before the first real generation.
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
